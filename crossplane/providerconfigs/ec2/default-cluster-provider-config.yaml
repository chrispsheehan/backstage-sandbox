apiVersion: aws.m.upbound.io/v1beta1
kind: ClusterProviderConfig
metadata:
  name: default
spec:
  credentials:
    # None leaves authentication to the AWS SDK credential chain, which obtains
    # temporary credentials from the EC2 instance profile through IMDSv2.
    source: None
