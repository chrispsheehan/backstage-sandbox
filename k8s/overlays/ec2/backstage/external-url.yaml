apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: backstage
spec:
  template:
    spec:
      containers:
        - name: backstage
          env:
            - name: APP_BASE_URL
              value: http://backstage.chrispsheehan.com
